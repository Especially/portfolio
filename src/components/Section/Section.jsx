import React from 'react';

const Section = ({title}) => {


    return (
        <section className="container__section">
          <h1>{title}</h1>
        </section>
    );
}

export default Section;